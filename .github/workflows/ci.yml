name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-go@v7
        with:
          go-version-file: go.mod
          # This module has zero dependencies, so there is no go.sum to hash
          # and nothing for the module cache to restore. Turn this back on if
          # dependencies are ever added.
          cache: false

      - name: Check formatting
        run: |
          unformatted="$(gofmt -l .)"
          if [ -n "$unformatted" ]; then
            echo "These files need gofmt:"
            echo "$unformatted"
            exit 1
          fi

      - name: Vet
        run: go vet ./...

      - name: Test
        run: go test -race -count=1 ./...

      - name: Build
        run: go build -trimpath -ldflags "-s -w" -o love .

      # Runs the real binary over real HTTP against a local mock server, so the
      # full pipeline is covered in CI without an API key or any spend.
      - name: End-to-end against a mock API
        run: LOVE_BIN=./love python3 scripts/e2e_mock_check.py
