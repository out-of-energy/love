# TODO：把「背单词」变成一个创作循环

> 来源：用户 2026-09-13 的口述计划，本文是润色后的版本。
> 原始想法：*"如何让背单词变成一项收益？薅羊毛？使用各种 Seedance / Wan 每日额度，
> 系统给你提供……你对今日所学单词的感悟、你的想象……我会拿着你的奇思妙想做成专业的
> gen video prompt，你用自己的 AI 视频引擎生成视频，推送到各大平台换取流量。"*

---

## 0. 一句话

**你对一个单词的私人想象，是这个系统里唯一无法被 AI 批量生产的东西。**

系统负责把它加工成能发出去的短视频；视频是奖励和作品，收益是副产品。

---

## 1. 为什么这不是噱头（学习上的依据）

先说清楚：这件事即使一分钱收益都没有，也值得做。因为"自己写对某个词的理解"是已知最强的记忆手段之一，而现在的项目**恰好缺这一步**。

| 现有环节 | 认知动作 | 强度 |
|---|---|---|
| 邮件里读 ELI5 / 例句 | 再认（recognition） | 弱 |
| `love --review` 自评 1–4 | 提取练习（retrieval） | 中 |
| **写自己的奇思妙想** | **产出 + 精细编码 + 自我参照** | **强** |

- **产出优于再认**：写一句自己的句子，比读十句别人的句子记得牢。
- **自我参照效应**：与"我"有关的材料，记忆成绩显著更高。你的奇思妙想天然是自我参照的。
- **情绪与意象**：越离奇越好记。而"离奇"正是 AI 很难替你生成的——它只能生成平均值。

所以这条链路的顺序是对的：**你先写，系统再放大**。反过来（系统生成、你只点赞）就退化成了又一个 AI 内容农场。

> 收益那条线是概率性的；记忆那条线是确定的。**先把确定的那条做出来。**

---

## 2. 与现有项目怎么接

现有能力（已上线）：

```
love <词>                 查词 → words.jsonl（22 个词）
love --review             复习 → reviews.jsonl（4 条事件）
love --daily              生成并发送邮件 → generated.jsonl（扩展层）
love --install-schedule   每天 07:30 / 12:30 / 20:30 自动推送
```

已有的三份数据，正好是这个新循环的输入：

| 数据 | 怎么用 |
|---|---|
| `words.jsonl` | 选哪个词做创作 |
| `generated.jsonl`（meaning / examples / scene / dialogue） | 给视频模型当上下文，保证画面和词义不跑偏 |
| `reviews.jsonl` | **挑"老是记不住"的词**——那才是最好的创作素材 |

**第三条是被低估的**：一个词你反复答错，说明它缺一个强关联。而你为它编一个荒诞画面，恰好补上。创作素材不该从"今天新学的词"里随机挑，**该从"你反复忘记的词"里挑**。

---

## 3. 核心循环

```
① 选题     love --create              → 从"反复忘记的词"里挑一个
② 你写     一段话、一个画面、一个梗     → 存 prompts.jsonl
③ 加工     你的文字 + 词义 → 专业视频 prompt（复用 internal/llm）
④ 生成     丢给视频模型（即梦 / 万相）  → videos/<word>.mp4
⑤ 发布     你手动发（见 §5 的原因）     → 抖音 / 小红书 / 视频号
⑥ 回收     播放/点赞/评论数回写         → 反哺选题（哪类想象受欢迎）
```

关键分工：

- **② 必须由人做，且系统不得代劳。** 这是全部价值所在，也是学习发生的地方。
- **③ 系统只做翻译**：把口语化的想象翻译成模型能执行的镜头语言。
- **④⑤ 是管道**，可以换供应商、可以失败、可以降级。
- **⑥ 是可选的**，没有它循环照样成立。

---

## 4. 分阶段

### P0 — 创作入口（最小可用，不依赖任何视频 API）

```bash
love --create                 # 挑一个该创作的词并提示你写
love --create --word maintain # 指定词
```

- 新增 `prompts.jsonl`：`{word_id, word, idea, created_at}`
- 选题规则：优先"错误次数最多且尚未创作过"的词
- 输入方式：`$EDITOR` 打开临时文件写（多行、可反复修改）
- **此阶段不接任何外部服务**，先把"你愿意写"这件事验证掉

**验收**：连续 7 天，每天真的写出一条。

> 如果这一步做不到，后面所有阶段都没有意义——因为到那时系统只能替你生成想象，而那就不是这个产品了。

### P1 — prompt 加工

```bash
love --create --prompt        # 把 prompts.jsonl 里未加工的条目转成视频 prompt
```

- 复用 `internal/llm`（已有重试、JSON 提取、契约校验）
- 输出结构化 prompt：`{shot, subject, action, style, duration, negative}`
- **必须注入词义约束**：把 `generated.jsonl` 的 `scene` 一起给模型，避免画面和词义无关
- 新增字段存回 `prompts.jsonl`，保留原始 `idea`（可追溯、可重做）

### P2 — 视频生成（免费额度）

```bash
love --create --render        # 调用配置的视频后端，产出 mp4
```

- provider 接口（照 `internal/ai` 的 `Provider` 形状）
- **额度记账**：新增 `quota.json` 记录当日已用次数，接近上限时告警而不是静默失败
- 每日额度是**稀缺资源**，所以要按优先级花：先给"反复忘记的词"
- 失败降级：生成不出来就只输出 prompt，不让整条链断掉

### P3 — 发布（**先手动**）

```bash
love --create --export        # 导出 mp4 + 标题 + 话题标签 到一个目录
```

**不做自动发布**，原因见 §5。先手动发 20 条，验证"有没有人看"，再决定要不要投入自动化。

### P4 — 回收（可选）

- 手动回填播放/互动数据，或读平台导出
- 反哺 §3 的⑥：哪类想象有反馈 → 影响选题权重
- **不要**在这一阶段之前就开始做数据看板

---

## 5. 前提与风险（已做事实核查）

### ⚠️ 「薅羊毛」这条链是脆的

| 事实 | 影响 |
|---|---|
| 即梦官方**每日赠 66 积分**（约 66 次生成） | 额度真实存在，但这是**产品获客手段，不是长期承诺**，随时可能取消 |
| 社区封装（如 [jimeng-free-api-all](https://github.com/laixiao/jimeng-free-api-all)）走的是网页 `sessionid` | **非官方接口**，违反服务条款的风险由你承担，账号可能被封 |
| 阿里云百炼的万相是**按量计费**（约 ¥0.37/秒，[计费页](https://help.aliyun.com/zh/model-studio/model-pricing)） | 官方 API 不免费；走官方就是花钱买确定性 |

**结论**：把免费额度当成"省钱的加速器"，**不要**当成系统的地基。P0–P1 完全不依赖它，这是刻意的。

### ⚠️ 自动发布到平台基本走不通

抖音 / 小红书 / 视频号**没有面向个人的开放发布 API**。第三方"一键分发"工具要么走模拟登录（同样封号风险），要么需要企业资质。

所以 P3 **默认手动**。手动发布还有个好处：你会真的看一眼数据。

### ⚠️ 这条链路打破了 local-first

整个项目此前承诺"词库始终在你自己机器上"。这一步会把：

- 你对某个词的**私人联想**（可能是很私人的东西）
- 生成的视频 prompt

发送给视频模型服务商，以及发布到公开平台。

**这是新的隐私边界，必须显式告知，不能默认开启。** 建议：`--create` 首次运行时明确说明"你写的内容会离开本机"，并可选择跳过 P2 只留本地 prompt。

### ⚠️ 内容审核

你的奇思妙想越离奇，越可能被模型的安全策略拒绝。要有"被拒 → 换个说法"的重试路径，而不是报错结束。

### 收益预期

**收益是概率性的，且大概率是零。** 这条线唯一确定的产出是：

1. 你为每个词写了一段东西（记忆效果，确定）
2. 你有一批自己的作品（作品集，确定）
3. 曝光与收益（不确定）

如果 1 和 2 对你有价值，这个功能就成立；如果只有 3，那它不成立。

---

## 6. 明确不做

- ❌ 系统替你生成想象（那就不是你的了，学习效果也没了）
- ❌ 自动发布到平台（P3 默认手动）
- ❌ 多平台数据看板（在验证有人看之前）
- ❌ 把免费额度当架构前提
- ❌ 在 P0 验证通过之前做 P1

---

## 7. 待确认

1. **视频后端**：先用即梦（免费但非官方、有风险），还是直接用阿里云百炼（付费但合规）？
2. **隐私**：`--create` 写的内容要发给第三方模型——接受吗？还是先只做本地 prompt（P0+P1）？
3. **发布平台**：主发哪个？（决定 prompt 的风格与时长，竖屏 15 秒还是横屏）
4. **收益目标**：你期待的是"有人看"还是"真能变现"？这决定 P3/P4 投入多少。
5. **是否独立命令**：按现有语法 `love --create`，还是拆成独立工具？（创作和背单词的使用场景差别很大）
